<UserControl
    x:Class="ChatMessageApp.UserControls.ChatSeprator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="chatSeprator"
    Margin="0,5,0,0"
    d:Background="White"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Separator
            Grid.Column="0"
            Height="0.8"
            Background="#DADADA" />
        <TextBlock
            Grid.Column="1"
            Margin="13,0"
            Background="#B7B7B7"
            FontSize="11"
            Text="{Binding ElementName=chatSeprator, Path=Title}" />
        <Separator
            Grid.Column="2"
            Height="0.8"
            Background="#DADADA" />
    </Grid>
</UserControl>
